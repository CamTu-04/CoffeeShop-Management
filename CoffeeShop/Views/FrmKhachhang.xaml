<Window x:Class="CoffeeShop.Views.FrmKhachhang"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Quản lý Khách hàng"
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F8FAFC"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#000" Opacity="0.1" BlurRadius="10" ShadowDepth="2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="SuccessButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="#EF4444"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="WarningButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="#F59E0B"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="#E2E8F0"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="TitleTextBlock" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="SectionHeaderTextBlock" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
        <Style x:Key="FormTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

    </Window.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="White"
                CornerRadius="8"
                BorderBrush="#E2E8F0"
                BorderThickness="1"
                Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Color="#000" Opacity="0.05" ShadowDepth="2"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Danh sách khách hàng"
                           Style="{StaticResource TitleTextBlock}"/>

                <Grid Grid.Row="1" Margin="0,0,0,15">
                    <TextBox x:Name="txtTimKiem"
                             Height="38"
                             VerticalContentAlignment="Center"
                             Padding="10,0"
                             Text="Tìm kiếm theo tên hoặc SĐT..."
                             Foreground="Gray"
                             GotFocus="txtTimKiem_GotFocus"
                             LostFocus="txtTimKiem_LostFocus"
                             TextChanged="txtTimKiem_TextChanged"/>
                </Grid>

                <DataGrid x:Name="dgKhachHang"
                          Grid.Row="2"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          RowHeight="40"
                          FontSize="14"
                          AlternatingRowBackground="#F8FAFC"
                          Background="White"
                          BorderBrush="#E2E8F0"
                          BorderThickness="1"
                          SelectionMode="Single"
                          CanUserAddRows="False"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#E2E8F0"
                          SelectionChanged="dgKhachHang_SelectionChanged">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã KH" Binding="{Binding MaKH}" Width="0.8*"/>
                        <DataGridTextColumn Header="Tên khách hàng" Binding="{Binding TenKH}" Width="2*"/>
                        <DataGridTextColumn Header="Số điện thoại" Binding="{Binding SoDienThoai}" Width="1.5*"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="1.5*"/>
                        <DataGridTextColumn Header="Địa chỉ" Binding="{Binding DiaChi}" Width="2*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <GridSplitter Grid.Column="1"
                      Width="6"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Stretch"
                      Background="Transparent"
                      ResizeBehavior="PreviousAndNext"/>

        <Border Grid.Column="2"
                Background="White"
                CornerRadius="8"
                BorderBrush="#E2E8F0"
                BorderThickness="1"
                Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Color="#000" Opacity="0.05" ShadowDepth="2"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="lblFormTitle"
                           Grid.Row="0"
                           Text="Thông tin khách hàng"
                           Style="{StaticResource TitleTextBlock}"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="spForm" IsEnabled="False">

                        <TextBlock Text="Mã khách hàng" Style="{StaticResource FormLabel}" Margin="0,10,0,5"/>
                        <TextBox x:Name="txtMaKH" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Text="Tên khách hàng (*)" Style="{StaticResource FormLabel}" Margin="0,10,0,5"/>
                        <TextBox x:Name="txtTenKH" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Text="Số điện thoại (*)" Style="{StaticResource FormLabel}" Margin="0,10,0,5"/>
                        <TextBox x:Name="txtSoDienThoai" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Text="Email" Style="{StaticResource FormLabel}" Margin="0,10,0,5"/>
                        <TextBox x:Name="txtEmail" Style="{StaticResource FormTextBox}"/>

                        <TextBlock Text="Địa chỉ" Style="{StaticResource FormLabel}" Margin="0,10,0,5"/>
                        <TextBox x:Name="txtDiaChi" 
                                 Style="{StaticResource FormTextBox}" 
                                 Height="100" 
                                 TextWrapping="Wrap" 
                                 AcceptsReturn="True"
                                 VerticalContentAlignment="Top"/>

                    </StackPanel>
                </ScrollViewer>

                <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,15,0,0">
                    <WrapPanel HorizontalAlignment="Right">
                        <Button x:Name="btnThem" Content="Thêm" Style="{StaticResource SuccessButtonStyle}" Click="btnThem_Click" RenderTransformOrigin="-1.07,0.57" Width="99">
                            <Button.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform/>
                                    <SkewTransform/>
                                    <RotateTransform Angle="-0.324"/>
                                    <TranslateTransform X="0.005" Y="-5.553"/>
                                </TransformGroup>
                            </Button.RenderTransform>
                        </Button>
                        <Button x:Name="btnLuu" Content="Lưu" Style="{StaticResource PrimaryButtonStyle}" Click="btnLuu_Click" Width="86"/>
                        <Button x:Name="btnXoa" Content="Xóa" Style="{StaticResource DangerButtonStyle}" Click="btnXoa_Click" Width="79"/>
                    </WrapPanel>
                    <WrapPanel HorizontalAlignment="Right">
                        <Button x:Name="btnHuy" Content="Hủy" Style="{StaticResource SecondaryButtonStyle}" Click="btnHuy_Click" Width="138"/>
                        <Button x:Name="btnLamMoi" Content="Làm mới DS" Style="{StaticResource WarningButtonStyle}" Click="btnLamMoi_Click" Width="137"/>
                    </WrapPanel>
                </StackPanel>

            </Grid>
        </Border>
    </Grid>
</Window>